services:
  - type: web
    name: telegram-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    envVars:
      - key: TELEGRAM_TOKEN
        value: ضع_التوكن_هنا
      - key: CHAT_ID
        value: ضع_الشات_آي_دي_هنا